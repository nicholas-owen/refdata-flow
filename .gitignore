#autogenerated
.claude/
docs/
# Request files. Both are yours, neither is tracked: requests_raw.csv is the list
# you edit and would collide with every pull if it were, and requests_resolved.csv
# is the resolver's cache. The tracked starting point is
# requests/requests_raw.csv.example, which this pattern deliberately does not match.
requests/*.csv
requests.csv
test*.py
docs_private/
refdata-flow-archive/
# Python virtual environment (created by run_pipeline.sh)
.venv/
__pycache__/
# Nextflow runtime artifacts
work/
.nextflow/
.nextflow.log*

# Downloaded reference data / refgenie vault (default output dir)
data/

